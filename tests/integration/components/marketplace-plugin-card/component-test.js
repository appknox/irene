import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('marketplace-plugin-card', 'Integration | Component | marketplace plugin card', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
