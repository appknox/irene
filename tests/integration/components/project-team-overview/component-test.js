import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('project-team-overview', 'Integration | Component | project team overview', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
