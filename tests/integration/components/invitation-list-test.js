import { test, moduleForComponent } from 'ember-qunit';

moduleForComponent('invitation-list', 'Integration | Component | invitation list', {
  integration: true
});

test('it renders', assert => assert.ok(true));
