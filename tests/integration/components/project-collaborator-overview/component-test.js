import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('project-collaborator-overview', 'Integration | Component | project collaborator overview', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
