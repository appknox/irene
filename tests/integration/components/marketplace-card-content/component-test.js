import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('marketplace-card-content', 'Integration | Component | marketplace card content', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
