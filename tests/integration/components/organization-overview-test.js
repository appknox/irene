import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Integration | Component | organization overview', function(hooks) {
  setupTest(hooks);

  test('it renders', function(assert) {
    assert.ok(true);
  });
});
