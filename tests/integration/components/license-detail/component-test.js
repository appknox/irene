import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('license-detail', 'Integration | Component | license detail', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
