import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('marketplace-integration-card', 'Integration | Component | marketplace integration card', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
