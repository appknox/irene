import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('add-project-collaborator', 'Integration | Component | add project collaborator', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
