import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('marketplace-integration-list', 'Integration | Component | marketplace integration list', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
