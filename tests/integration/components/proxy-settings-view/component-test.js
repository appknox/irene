import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('proxy-settings-view', 'Integration | Component | proxy settings view', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
  assert.equal(this.$().text().trim(), '');
});
