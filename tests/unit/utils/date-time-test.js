import dateTime from 'irene/utils/date-time';
import { module, test } from 'qunit';

module('Unit | Utility | date time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateTime();
  assert.ok(result);
});
