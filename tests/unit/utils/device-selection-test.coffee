`import deviceSelection from '../../../utils/device-selection'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | device selection'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = deviceSelection()
  assert.ok result
