/* eslint-disable prettier/prettier */
import { module, test } from 'qunit';

module('Unit | Utility | trigger analytics', function() {
  test('it works', function(assert) {
    assert.ok(true);
  });
});
