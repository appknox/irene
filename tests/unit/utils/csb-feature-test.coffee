`import csbFeature from '../../../utils/csb-feature'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | csb feature'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = csbFeature()
  assert.ok true
