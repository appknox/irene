import installInspectlet from '../../../utils/install-inspectlet';
import { module, test } from 'qunit';

module('Unit | Utility | install inspectlet');

test('it works', function(assert) {
  const result = installInspectlet();
  assert.ok(true);
});
