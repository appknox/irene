import triggerHeadwayapp from 'irene/utils/trigger-headwayapp';
import { module, test } from 'qunit';

module('Unit | Utility | trigger headwayapp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = triggerHeadwayapp();
  assert.ok(result);
});
