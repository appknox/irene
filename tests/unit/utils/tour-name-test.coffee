`import tourName from '../../../utils/tour-name'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | tour name'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = tourName()
  assert.ok result
