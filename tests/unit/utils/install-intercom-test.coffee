`import installIntercom from '../../../utils/install-intercom'`
`import { module, test } from 'qunit'`

module 'installIntercom'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = installIntercom()
  assert.ok result
