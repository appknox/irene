`import imagePool from '../../../utils/image-pool'`
`import { module, test } from 'qunit'`

module 'imagePool'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = imagePool()
  assert.ok result
