`import installRaven from '../../../utils/install-raven'`
`import { module, test } from 'qunit'`

module 'installRaven'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = installRaven()
  assert.ok result
