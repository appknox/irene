import installIntercom from '../../../utils/install-intercom';
import { module, test } from 'qunit';

module('Unit | Utility | install intercom');

test('it works', function(assert) {
  const result = installIntercom();
  assert.ok(true);
});
