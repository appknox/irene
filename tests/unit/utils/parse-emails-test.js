import parseEmails from 'irene/utils/parse-emails';
import { module, test } from 'qunit';

module('Unit | Utility | parse emails');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseEmails();
  assert.ok(result);
});
