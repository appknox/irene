`import humanizeCvss from '../../../utils/humanize-cvss'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | humanize cvss'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = humanizeCvss()
  assert.ok result
