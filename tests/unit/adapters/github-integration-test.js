import { moduleFor, test } from 'ember-qunit';

moduleFor('adapter:github-integration', 'Unit | Adapter | github integration', {
});

test('it exists', function(assert) {
  const adapter = this.subject();
  assert.ok(adapter);
});
