import { moduleFor, test } from 'ember-qunit';

moduleFor('adapter:jira-integration', 'Unit | Adapter | jira integration', {
});

test('it exists', function(assert) {
  const adapter = this.subject();
  assert.ok(adapter);
});
