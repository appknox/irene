/* eslint-disable qunit/no-commented-tests */
// import { moduleForModel, test } from 'ember-qunit';
//
// moduleForModel('github-repo', 'Unit | Model | github repo', {
//   // Specify the other units that are required for this test.
//   needs: ['model:project']
// });
//
// test('should own a project', function(assert) {
//   let githubRepo = this.store().modelFor('github-repo');
//   const relationship = Ember.get(githubRepo, 'relationshipsByName').get('project');
//   assert.equal(relationship.key, 'project', 'has relationship with profile');
//   assert.equal(relationship.kind, 'belongsTo', 'kind of relationship is belongsTo');
// });
//
// test('should have the values', function(assert) {
//   let githubRepo = this.subject({account: "test", repo: "repo",
//     risk_threshold: 1})
//   assert.equal(githubRepo.get('account'), 'test', 'has value test');
//   assert.equal(githubRepo.get('repo'), 'repo', 'has value repo');
//   assert.equal(githubRepo.get('risk_threshold'), 1, 'has value 1');
// });
