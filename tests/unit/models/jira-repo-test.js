/* eslint-disable qunit/no-commented-tests */
// import { moduleForModel, test } from 'ember-qunit';
//
// moduleForModel('jira-repo', 'Unit | Model | jira repo', {
//   // Specify the other units that are required for this test.
//   needs: ['model:project']
// });
//
// test('should own a project', function(assert) {
//   let jiraRepo = this.store().modelFor('jira-repo');
//   const relationship = Ember.get(jiraRepo, 'relationshipsByName').get('project');
//   assert.equal(relationship.key, 'project', 'has relationship with profile');
//   assert.equal(relationship.kind, 'belongsTo', 'kind of relationship is belongsTo');
// });
//
// test('should have the values', function(assert) {
//   let jiraRepo = this.subject({project_key: "MFVA", project_name: "MFVA",
//     risk_threshold: 1})
//   assert.equal(jiraRepo.get('project_key'), 'MFVA', 'has value MFVA');
//   assert.equal(jiraRepo.get('project_name'), 'MFVA', 'has value MFVA');
//   assert.equal(jiraRepo.get('risk_threshold'), 1, 'has value 1');
// });
