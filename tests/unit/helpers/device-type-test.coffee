`import { deviceType } from 'irene/helpers/device-type'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | device type'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = deviceType 42
  assert.ok result
