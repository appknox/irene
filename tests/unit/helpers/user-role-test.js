
import { userRole } from 'irene/helpers/user-role';
import { module, test } from 'qunit';

module('Unit | Helper | user role');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userRole([42]);
  assert.ok(result);
});
