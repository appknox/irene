`import { planDuration } from 'irene/helpers/plan-duration'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | plan duration'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = planDuration 42
  assert.ok result
