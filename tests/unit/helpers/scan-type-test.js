
import { scanType } from 'irene/helpers/scan-type';
import { module, test } from 'qunit';

module('Unit | Helper | scan type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scanType([42]);
  assert.ok(result);
});

