
import { scrollTo } from 'irene/helpers/scroll-to';
import { module, test } from 'qunit';

module('Unit | Helper | scroll to');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scrollTo([42]);
  assert.ok(result);
});

