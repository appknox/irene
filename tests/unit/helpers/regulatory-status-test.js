
import { regulatoryStatus } from 'irene/helpers/regulatory-status';
import { module, test } from 'qunit';

module('Unit | Helper | regulatory status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = regulatoryStatus([42]);
  assert.ok(result);
});

