
import { riskText } from 'irene/helpers/risk-text';
import { module, test } from 'qunit';

module('Unit | Helper | risk text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = riskText([42]);
  assert.ok(result);
});

