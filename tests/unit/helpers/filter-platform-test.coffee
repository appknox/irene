`import { filterPlatform } from 'irene/helpers/filter-platform'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | filter platform'

# Replace this with your real tests.
test 'it works', (assert) ->
  # result = filterPlatform 42
  assert.ok true
