`import { versionText } from 'irene/helpers/version-text'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | version text'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = versionText 42
  assert.ok result
