machine:
  pre:
    - sudo curl --output /usr/local/bin/phantomjs https://s3.amazonaws.com/circle-downloads/phantomjs-2.1.1
  node:
    version: 6.5.0

dependencies:
  post:
    - npm install abbrev
    - npm rebuild node-sass
    - npm install -g bower
    - bower install

test:
  override:
    - ember test
  post:
    - bash <(curl -s https://codecov.io/bash) -t d2a87983-bdd8-4e12-9cb2-99e5607e648f
