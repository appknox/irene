{{page-title 'Billing'}}

{{#if @model.organization.showBilling}}
  {{#if @model.showLicense}}
    <LicenseDetail @license={{@model.license}} />
  {{else}}
    <OrganizationBilling />
  {{/if}}
{{/if}}