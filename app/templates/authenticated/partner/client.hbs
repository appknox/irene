{{page-title "Client Details"}}

<Partner::ClientsNav></Partner::ClientsNav>

<div class="padding-h-1 padding-t-1h padding-b-2">
  <LinkTo @route="authenticated.partner.clients">Clients</LinkTo> /
  {{#if this.model.isEmptyTitle}}
    <i>{{t 'noName'}}</i>
  {{else}}
    {{this.model.name}}
  {{/if}}
  {{#if this.isError}}
    error
  {{/if}}

  <Partner::ClientDetail @client={{this.model}}></Partner::ClientDetail>

  {{outlet}}
</div>
