{{page-title (t 'appDetails')}}

<PrivacyModule::AppDetails::Note />

<PageWrapper>
  <PrivacyModule::AppDetails::Header @app={{@model.privacyProject}} />

  {{#if @model.scanCompleted}}
    {{outlet}}
  {{/if}}
</PageWrapper>