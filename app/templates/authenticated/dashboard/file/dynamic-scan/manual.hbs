{{page-title (t 'manual')}}

<ProjectPreferencesOld::Provider
  @profileId={{@model.profileId}}
  @platform={{@model.projectPlatform}}
  @project={{@model.file.project}}
  @file={{@model.file}}
  as |dpContext|
>
  <FileDetails::DynamicScan::Manual
    @dpContext={{dpContext}}
    @file={{@model.file}}
    @profileId={{@model.profileId}}
  />
</ProjectPreferencesOld::Provider>