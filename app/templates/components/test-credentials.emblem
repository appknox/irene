h5
  = t "testCredentials"
form
    label for="email"
      = t "emailOrUsername"
    input type="text" name="email"

    label for="password"
      = t "password"
    input type="password" name="password"

    button.is-primary
      = t "saveCredentials"

=yield
