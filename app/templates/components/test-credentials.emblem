h5
  = t "testCredentials"

form.form-spacing
  .columns
    .column.is-one-quarter
      = input class="input" value=project.testUser placeholder=(t 'emailOrUsername') id="mp-test-username"
  .columns
    .column.is-one-quarter
      = input class="input" value=project.testPassword placeholder=(t "password") id="mp-test-password"

  button.is-primary click="saveCredentials" id="mp-save-credentials"
    = t "saveCredentials"
