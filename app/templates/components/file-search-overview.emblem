td
  = file.id
td
  = file.name
td.table-button
  = link-to 'authenticated.security.file' file.id class="mp-all-scans is-primary primary-button" tagName="button"
    | View
td
  button.is-primary.primary-button click="downloadApp" disabled=isDownloadingApp
    if isDownloadingApp
      .fa-font-size
        i.fa class="fa-spinner fa-spin"
      | &nbsp;
    | Download App
