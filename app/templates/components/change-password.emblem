form
    label for="currentPassword"
      = t "currentPassword"
    = input type="password" value=passwordCurrent

    label for="newPassword"
      = t "newPassword"
    = input type="password" value=passwordNew

    label for="confirmPassword"
      = t "confirmPassword"
    = input type="password" value=passwordConfirm

    button.is-primary click="changePassword"
      = t "changePassword"
