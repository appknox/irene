.columns
  .column.flex-center
    img src="images/logo.png" alt="Mobile application security"
.columns
  .column.flex-center
    = t "resetPassword"
.columns
  .column.flex-center
    form.login-form.recover-form

      label for="newPassword"
        = t "newPassword"
      = input class="input" type="password" value=password

      label for="confirmPassword"
        = t "confirmPassword"
      = input class="input" type="password" value=confirmPassword

      p.control.flex-center
        button.button.is-primary.login-button click="reset"
          = t "reset"
