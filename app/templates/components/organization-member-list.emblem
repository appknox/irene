.columns
  .column
    if hasObjects
      table.org-table
        tr
          th
            = t "user"
          th
            = t "role"
          th
            = t "teams"
        each sortedObjects as |member|
          = organization-member-overview member=member organization=organization
.columns
  .column
    if hasObjects
      .margin-top20
        = partial "paginate"
