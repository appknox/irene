form
  .control
    label for="currentPassword"
      = t "currentPassword"
    .columns
      .column.is-half
        = input class="input" type="password" value=passwordCurrent mpId="mp-current-password"

    label for="newPassword"
      = t "newPassword"
    .columns
      .column.is-half
        = input class="input" type="password" value=passwordNew mpId="mp-new-password"

    label for="confirmPassword"
      = t "confirmPassword"
    .columns
      .column.is-half
        = input class="input" type="password" value=passwordConfirm mpId="mp-confirm-password"

    button.is-primary.margin-top20 click="changePassword" mpId="mp-change-password"
      = t "changePassword"
