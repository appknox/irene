if invitations
  .columns
    .column.margin-left
      table.org-table
        tr
          th.uppercase-text
            = t "email"
          th.uppercase-text
            = t "team"
            | /
            = t "organization"
          th
            = t "actions"
        each invitations as |invitation|
          = pending-invitation invitation=invitation orgId=orgId
else
  .not-found
    h4
      = fa-icon "exclamation-triangle"
      | no invitations
