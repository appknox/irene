select.input.is-fullwidth.mp-select-language{action 'setLocale' on='change'}
  option selected="true" value=currentLocale.locale
    = currentLocale.localeString
  each otherLocales as |locale|
    option value=locale.locale
      = locale.localeString
