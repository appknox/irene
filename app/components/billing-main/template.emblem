// if model.isPaymentDone
//   = payment-details
// else
//   = pricing-cards pricingPlans=model.plans isJustifiedCenter=model.isJustifiedCenter

= pricing-cards pricingPlans=model.plans isJustifiedCenter=model.isJustifiedCenter
