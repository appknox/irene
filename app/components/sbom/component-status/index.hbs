<AkStack @spacing='1'>
  {{#each this.componentStatus as |status|}}
    <AkChip
      data-test-sbomComponent-status='{{status.label}}'
      @variant='semi-filled'
      @color={{status.color}}
      @label={{status.label}}
      @size='small'
    />
  {{/each}}
</AkStack>