<AkTabs as |Akt|>
  {{#each this.tabs as |item|}}
    <Akt.tabItem
      @id={{item.id}}
      @route={{item.route}}
      @currentWhen={{item.activeRoutes}}
    >
      {{item.label}}
    </Akt.tabItem>
  {{/each}}
</AkTabs>